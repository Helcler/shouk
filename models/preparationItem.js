class PreparationItem {
    constructor(itemId, quantity, itemPrice, itemTitle, sum) {
        this.itemId = itemId;
        this.quantity = quantity;
        this.itemPrice = itemPrice;
        this.itemTitle = itemTitle;
        this.sum = sum;
    }
}
export default PreparationItem;